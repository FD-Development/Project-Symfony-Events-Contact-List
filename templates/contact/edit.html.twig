{% include 'default/_form.html.twig' with {
    page_title: 'title.contact_edit'|trans({'%id%': contact.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'contact_index',
} %}