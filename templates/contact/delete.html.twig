{% include 'default/_form.html.twig' with {
    page_title: 'title.contact_delete'|trans({'%id%': contact.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'contact_index',
} %}
